{
  "contractName": "MyFlashLoanReceiver",
  "contractSource": "// SPDX-License-Identifier: MIT\n// Compatible with OpenZeppelin Contracts ^5.7.0 and Aave v3 Core ^1.19.0\npragma solidity ^0.8.27;\n\nimport {FlashLoanSimpleReceiverBase} from \"@aave/core-v3/contracts/flashloan/base/FlashLoanSimpleReceiverBase.sol\";\nimport {IPoolAddressesProvider} from \"@aave/core-v3/contracts/interfaces/IPoolAddressesProvider.sol\";\nimport {Ownable} from \"@openzeppelin/contracts/access/Ownable.sol\";\nimport {IERC20} from \"@openzeppelin/contracts/token/ERC20/IERC20.sol\";\nimport {SafeERC20} from \"@openzeppelin/contracts/token/ERC20/utils/SafeERC20.sol\";\nimport {Pausable} from \"@openzeppelin/contracts/utils/Pausable.sol\";\n\n/// @title MyFlashLoanReceiver\n/// @notice Aave v3 flashLoanSimple receiver generated by HARNESS. The callback is gated to the Pool and to loans this contract itself initiated.\ncontract MyFlashLoanReceiver is FlashLoanSimpleReceiverBase, Ownable, Pausable {\n    using SafeERC20 for IERC20;\n\n    /// @dev Strictly typed. Deliberately carries no `bytes` payload and no call target:\n    /// arbitrary calldata decoded here is finding AAVE-FL-002 (Mimo `aggregatorSwap`).\n    struct FlashParams {\n        uint256 minAmountOut;\n        uint256 deadline;\n        address router;\n    }\n\n    /// @notice The single underlying this receiver is allowed to borrow.\n    IERC20 public immutable ASSET;\n\n    error NotPool(address caller);\n    error NotSelfInitiated(address initiator);\n    error UnexpectedAsset(address asset);\n    error DeadlinePassed(uint256 deadline);\n    error RepaymentShortfall(uint256 owed);\n    event FlashLoanExecuted(address indexed asset, uint256 amount, uint256 premium);\n    mapping(address router => bool allowed) public allowedRouters;\n    error RouterNotAllowed(address router);\n    error MissingSlippageBound();\n    event RouterAllowed(address indexed router, bool allowed);\n    event Swept(address indexed token, address indexed to, uint256 amount);\n\n    constructor(address addressesProvider, address asset_, address initialOwner)\n        FlashLoanSimpleReceiverBase(IPoolAddressesProvider(addressesProvider))\n        Ownable(initialOwner)\n    {\n        ASSET = IERC20(asset_);\n    }\n\n    /// @notice Aave calls this after sending `amount` of `asset` to this contract.\n    /// @dev THREE GATES, all required. Removing any one of them is a Critical finding:\n    /// @dev  1. AAVE-FL-001 — caller must be the Pool.\n    /// @dev  2. AAVE-FL-001 — the loan must be one WE initiated. Without this, anyone\n    /// @dev     can call `Pool.flashLoan` naming this contract as receiver and Aave will\n    /// @dev     dutifully invoke this callback for them (DODO MarginTrading, Mimo SuperVault).\n    /// @dev  3. AAVE-FL-002 — `params` decodes to a fixed struct, never to a call target.\n    function executeOperation(address asset, uint256 amount, uint256 premium, address initiator, bytes calldata params)\n        external\n        returns (bool)\n    {\n        if (msg.sender != address(POOL)) revert NotPool(msg.sender);\n        if (initiator != address(this)) revert NotSelfInitiated(initiator);\n        if (asset != address(ASSET)) revert UnexpectedAsset(asset);\n\n        FlashParams memory p = abi.decode(params, (FlashParams));\n        if (block.timestamp > p.deadline) revert DeadlinePassed(p.deadline);\n\n        if (!allowedRouters[p.router]) revert RouterNotAllowed(p.router);\n        if (p.minAmountOut == 0) revert MissingSlippageBound();\n\n        // >>> Your strategy goes here. It must leave `amount + premium` of ASSET on\n        // >>> this contract by the time this function returns.\n        _executeStrategy(amount, premium, p);\n\n        uint256 owed = amount + premium;\n        if (ASSET.balanceOf(address(this)) < owed) revert RepaymentShortfall(owed);\n\n        // §9.1: Aave PULLS the repayment. Approve, never transfer.\n        ASSET.forceApprove(address(POOL), owed);\n        emit FlashLoanExecuted(asset, amount, premium);\n        return true;\n    }\n\n    /// @dev Override point. Deliberately a no-op: HARNESS never emits a strategy it\n    /// @dev cannot prove safe. The gates above hold regardless of what replaces this.\n    function _executeStrategy(uint256 amount, uint256 premium, FlashParams memory p)\n        internal\n    {\n        // Funds are already on this contract. Deploy them, then return with at least\n        // `amount + premium` of ASSET on hand.\n    }\n\n    /// @notice Entry point. Only an authorised caller may put this contract on the hook.\n    /// @dev AAVE-FL-013 — this contract is not meant to hold an idle balance between\n    /// @dev calls; an idle balance is what repays an attacker's loan.\n    /// @dev §8.1 — `flashLoanSimple` is used deliberately: the multi-asset `flashLoan`\n    /// @dev with interestRateModes = 2 OPENS DEBT instead of repaying.\n    function initiateFlashLoan(uint256 amount, FlashParams calldata p)\n        external\n        onlyOwner\n        whenNotPaused\n    {\n        POOL.flashLoanSimple(address(this), address(ASSET), amount, abi.encode(p), 0);\n    }\n\n    function pause() public onlyOwner {\n        _pause();\n    }\n\n    function unpause() public onlyOwner {\n        _unpause();\n    }\n\n    /// @notice AAVE-SWP-014 — swaps may only route through vetted contracts.\n    function setRouterAllowed(address router, bool allowed) external onlyOwner {\n        allowedRouters[router] = allowed;\n        emit RouterAllowed(router, allowed);\n    }\n\n    /// @notice AAVE-VLT-009 — recover tokens that arrived outside the strategy.\n    /// @dev Safe here precisely because this contract holds no principal between\n    /// @dev flash loans (AAVE-FL-013). On the vault preset this must exclude the asset.\n    function sweep(address token, address to) external onlyOwner {\n        uint256 balance = IERC20(token).balanceOf(address(this));\n        IERC20(token).safeTransfer(to, balance);\n        emit Swept(token, to, balance);\n    }\n}\n",
  "attackTestSource": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.27;\n\nimport {Test} from \"forge-std/Test.sol\";\nimport {IPool} from \"@aave/core-v3/contracts/interfaces/IPool.sol\";\nimport {IPoolAddressesProvider} from \"@aave/core-v3/contracts/interfaces/IPoolAddressesProvider.sol\";\nimport {IERC20} from \"@openzeppelin/contracts/token/ERC20/IERC20.sol\";\nimport {MyFlashLoanReceiver} from \"../src/MyFlashLoanReceiver.sol\";\n\n/// @title MyFlashLoanReceiverAttackTest\n/// @notice Generated by HARNESS. Every test below is derived from a documented\n/// @notice incident, cited in the comment above it. These run against real Aave\n/// @notice on a mainnet fork — not against a mock.\ncontract MyFlashLoanReceiverAttackTest is Test {\n    IPoolAddressesProvider internal constant PROVIDER =\n        IPoolAddressesProvider(0x2f39d218133AFaB8F2B819B1066c7E434Ad94E9e);\n    address internal constant ASSET = 0xA0b86991c6218b36c1d19D4a2e9Eb0cE3606eB48;\n    address internal constant ATOKEN = 0x98C23E9d8f34FEFb1B7BD6a91B7FF122F4e16F5c;\n\n    IPool internal POOL;\n    MyFlashLoanReceiver internal harness;\n    address internal owner;\n\n    function setUp() public {\n        // No fork block is pinned yet: the Tenderly Virtual Environment forks at\n        // latest until TENDERLY_FORK_BLOCK is published. Setting that env var pins\n        // both sides without regenerating anything.\n        uint256 forkBlock = vm.envOr(\"TENDERLY_FORK_BLOCK\", uint256(0));\n        if (forkBlock == 0) {\n            vm.createSelectFork(vm.envString(\"MAINNET_RPC_URL\"));\n        } else {\n            vm.createSelectFork(vm.envString(\"MAINNET_RPC_URL\"), forkBlock);\n        }\n\n        // §9.1: resolve the Pool through the provider, never hardcode it.\n        POOL = IPool(PROVIDER.getPool());\n        owner = makeAddr(\"owner\");\n        harness = new MyFlashLoanReceiver(address(PROVIDER), ASSET, owner);\n    }\n\n    /// @dev A valid, benign FlashParams. Attack tests deviate from this deliberately.\n    function _defaultParams() internal pure returns (bytes memory) {\n        return abi.encode(\n            MyFlashLoanReceiver.FlashParams({\n                minAmountOut: 1,\n                deadline: type(uint256).max,\n                router: address(0)\n            })\n        );\n    }\n\n    /// AAVE-FL-001 — Flash-loan callback not gated to Pool + initiator\n    /// AAVE-FL-001 — anyone may call Pool.flashLoanSimple naming OUR contract as the\n    /// receiver. Aave then invokes our callback on their behalf, with their params.\n    /// Checking msg.sender == Pool is NOT enough: the Pool really is the caller.\n    /// This is the bug that drained DODO MarginTrading and Mimo SuperVault.\n    /// @dev Incident: DODO MarginTrading — https://github.com/sherlock-audit/2023-01-derby-judging/issues/150\n    /// @dev Runnable PoC: https://github.com/sanbir/evm-hack-registry/tree/main/2021-03-dodo_flashloan_exp\n    /// @dev Incident: Mimo SuperVault (Code4rena H-02) — https://code4rena.com/reports/2022-08-mimo\n    function test_RejectsThirdPartyInitiator() public {\n        address attacker = makeAddr(\"attacker\");\n\n        vm.prank(attacker);\n        vm.expectRevert(abi.encodeWithSignature(\"NotSelfInitiated(address)\", attacker));\n        POOL.flashLoanSimple(address(harness), ASSET, 1_000e6, _defaultParams(), 0);\n    }\n\n    /// AAVE-FL-002 — Attacker-controlled calldata executed from callback params\n    /// AAVE-FL-002 — the tutorial pattern decodes `params` into (address target,\n    /// bytes data) and calls it, turning finding 001 from a revert into a drain.\n    /// Here `params` decodes to a fixed struct with no call target, so hostile bytes\n    /// cannot become an external call. Asserted even with both other gates satisfied.\n    /// @dev Incident: Mimo SuperVault aggregatorSwap(dexTxData) — https://code4rena.com/reports/2022-08-mimo\n    function test_RejectsAttackerControlledParams() public {\n        // Worst case: the attacker somehow satisfies the caller AND initiator gates.\n        vm.prank(address(POOL));\n        vm.expectRevert();\n        harness.executeOperation(ASSET, 1_000e6, 5e5, address(harness), hex\"deadbeef\");\n    }\n}",
  "deployScriptSource": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.27;\n\nimport {Script, console} from \"forge-std/Script.sol\";\nimport {MyFlashLoanReceiver} from \"../src/MyFlashLoanReceiver.sol\";\n\n/// @notice Generated by HARNESS. Deploys MyFlashLoanReceiver against Aave v3 on Ethereum.\ncontract DeployMyFlashLoanReceiver is Script {\n    address constant POOL_ADDRESSES_PROVIDER = 0x2f39d218133AFaB8F2B819B1066c7E434Ad94E9e;\n    address constant ASSET = 0xA0b86991c6218b36c1d19D4a2e9Eb0cE3606eB48;\n\n    function run() external {\n        uint256 pk = vm.envUint(\"PRIVATE_KEY\");\n        address deployer = vm.addr(pk);\n\n        vm.startBroadcast(pk);\n        MyFlashLoanReceiver deployed = new MyFlashLoanReceiver(POOL_ADDRESSES_PROVIDER, ASSET, deployer);\n        vm.stopBroadcast();\n\n        console.log(\"MyFlashLoanReceiver deployed at\", address(deployed));\n    }\n}",
  "remappings": [
    "@openzeppelin/contracts/=lib/openzeppelin-contracts/contracts/",
    "@aave/core-v3/=lib/aave-v3-core/",
    "@aave/periphery-v3/=lib/aave-v3-periphery/",
    "forge-std/=lib/forge-std/src/"
  ],
  "appliedFindingIds": [
    "AAVE-DEP-015",
    "AAVE-FL-002",
    "AAVE-SWP-014",
    "AAVE-FL-001",
    "AAVE-FL-013",
    "AAVE-RISK-010",
    "AAVE-VLT-009"
  ]
}
